# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Local MongoDB
MONGO_URI=mongodb://localhost:27017/agentx

# MongoDB Atlas (Cloud) - Uncomment and configure for cloud deployment
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/agentx?retryWrites=true&w=majority

# JWT Authentication Secrets
# IMPORTANT: Change these to strong, unique values in production!
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=asdfsdbgerw4fecdsve
JWT_REFRESH_SECRET=cwvf3rsdvr3vweafr3rw

# CORS Configuration (Frontend URL)
# Comma-separated list of allowed origins
FRONTEND_URL=http://localhost:3000
